# Application Configuration
APP_NAME=jacob
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# Temporal Cloud Configuration with API Key Authentication
TEMPORAL_TLS_ENABLED=true
TEMPORAL_HOST=your-namespace.tmprl.cloud
TEMPORAL_PORT=7233
TEMPORAL_NAMESPACE=your-namespace
TEMPORAL_TASK_QUEUE=party-queue
TEMPORAL_API_KEY=your-api-key-here

# Google Maps API Key
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Device Whitelist (comma-separated device IDs, leave empty to allow all)
# Only blocks write operations (start/stop/update party), NOT read operations (query state)
# Example: ALLOWED_DEVICE_IDS=uuid1,uuid2,uuid3
ALLOWED_DEVICE_IDS=

# ============================================================================
# Web Push Notifications (VAPID)
# ============================================================================
# Optional: Enable web push notifications for party events
# If not configured, workflows will run but skip push notifications
#
# Generate keys using:
#   swift run App generate-vapid-keys
# Or manually:
#   - Generate P256 key pair
#   - Convert to base64-encoded raw representation
#
# VAPID_PUBLIC_KEY=base64-encoded-public-key
# VAPID_PRIVATE_KEY=base64-encoded-private-key
# VAPID_SUBJECT=mailto:admin@jacob.party
# ============================================================================

# ============================================================================
# API Key Authentication vs mTLS
# ============================================================================
# This configuration uses API key authentication (new feature!)
# - Simpler: Just set TEMPORAL_API_KEY
# - No certificate management needed
#
# If you still need mTLS certificate authentication, comment out TEMPORAL_API_KEY
# and uncomment these instead:
# TEMPORAL_CLIENT_CERT=/path/to/client.pem
# TEMPORAL_CLIENT_KEY=/path/to/client.key
#
# For cloud deployments (DO, Heroku), you can use certificate content:
# TEMPORAL_CLIENT_CERT_CONTENT=<paste PEM content>
# TEMPORAL_CLIENT_KEY_CONTENT=<paste key content>
# ============================================================================

# ============================================================================
# Local Development Configuration (plaintext, no TLS)
# ============================================================================
# For local development with temporal server start-dev:
# TEMPORAL_TLS_ENABLED=false
# TEMPORAL_HOST=127.0.0.1
# TEMPORAL_PORT=7233
# TEMPORAL_NAMESPACE=default
# TEMPORAL_TASK_QUEUE=party-queue
# # TEMPORAL_API_KEY not needed for local dev
# ============================================================================
